# v0.6 验证记录

更新：2026-09-07。新工作流的证据与 v0.5 分开记录。

## 已完成的 6 页真实运行

来源：已保存的 Wang（2022）45 页论文，第 1–6 页。运行 `c1155ed8efec453c89862e34f5fed3bd`，每批 3 页、并发 2、官方订阅 `gpt-6-astra`、思考强度 `medium`。

五阶段通过，XeLaTeX 编译成功；无需修复请求，无手工修改产物。耗时约 198 秒，5 次应用调用：设置 1、转换 2、接缝 1、标题 1。实际输入 67,382 token，输出 9,387 token，合计 76,769；输出中包含 151 个推理 token。该运行发生于进一步关闭运行时技能目录之前，不能将其固定开销套用于后来请求。

## 程序和界面

26 项程序回归通过，包括 532 页分成 178 个连续批次、严格页面归属、已完成响应恢复复用、手动修复重试、原子幂等操作、最终 TeX 展示、真实用量统计和实际 XeLaTeX 编译。

旧界面当前页接口约 718 KB、2.77 秒，会携带全书片段。新版 bootstrap 约 6 KB，当前页内容与任务状态分开加载。首轮真实浏览器观察中运行页加载约 582 毫秒；连续 7 秒中原图／输入框节点保持不变、输入未被重置，原图重请求为 0。另在真实第 10 批从运行变为完成时验证，尚未提交的页码 `29` 未被覆盖。机器和缓存会影响耗时，该记录不代表所有设备的固定性能。

重启最新版服务后的实测：bootstrap 为 6,359 字节、8.38 毫秒；状态为 496 字节、2.91 毫秒；第 14 页最终 TeX 为 6,377 字节、2.68 毫秒。完整 PDF 和导出 ZIP 可读，ZIP 完整性检查通过。完成后的运行页约 580 毫秒打开。

浏览器测试不发起模型请求；程序测试中的替代模型仅验证程序逻辑。

## 工具范围检查

关闭模型工具、技能目录和多代理之后，另有一次首页标题及工具目录最小真实检查：输入 5,738、输出 153、合计 5,891 token；包含 78 个推理 token。工具列表是模型自述，不能代替权限探针。此前一次配置类型错误在运行时启动前失败，没有上游推理用量。

本机 Windows 原生沙箱跨目录读取探针失败。页面发送范围已实现，但操作系统级严格只读范围尚未通过验证；不得标为完成。详见 [工作流能力边界](workflow-v0.6.md)。

## 完整 45 页

来源：Wang（2022）论文全部 45 页。运行 `4d18bed400e04944b0958300cd8f2306`，修订 2，最终 `COMPLETED`。每批 3 页，15 个转换批次，运行并发 4，模型 `gpt-6-astra`，思考强度 `medium`。测试期间将连接并发从 2 调整到 4，结束后恢复为 2。

全书设置、转换、接缝、标题到最终编译由程序连续完成。没有手工修改正文，没有中途为这篇论文改写转换规则，没有局部修复请求。总耗时 899.8 秒，XeLaTeX 2 遍编译通过，输出 56 页 PDF。

| 用途 | 实际应用请求数 |
| --- | ---: |
| 全书设置 | 1 |
| 视觉转换 | 15 |
| 接缝 | 7 |
| 标题层级 | 1 |
| 修复 | 0 |
| 合计 | 24 |

实际输入 **254,797** token，输出 **78,093** token，合计 **332,890** token。输入包含缓存读取 16,896 token；输出包含推理 94 token，二者均不能再次加到总量。全部 24 次都取得了真实用量回执，未出现未知或失败请求。

7 次接缝调用中 3 次返回实际修改，4 次认为无需修改；其余边界由首尾闭合状态直接拼接。31 条标题完成层级映射。回执只有用户输入、推理和最终消息，没有工具调用项。

程序核对物理页 1–45 按序唯一覆盖。抽查原页与输出的公式 (2.1)、(2.2)、章节标题及末页信息；单位和邮箱 `DZ1721003@smail.nju.edu.cn` 均保留。这里没有逐行检查全部数学论证，不能由编译成功推出无损还原。

编译日志仍有 20 处 overfull 排版警告，最大约 69.6 pt；固定模板没有为这些警告自动请求模型重写数学内容。输出 TeX 可继续排版。页覆盖、锚点和编译是程序检查，不是完整语义证明。

原始请求、回执、设置与产物保存在 `.bookanalyst/runs/4d18bed400e04944b0958300cd8f2306/`；汇总是 `.bookanalyst/v6-full-validation.json`。浏览器运行中报告为 `.bookanalyst/rebuild-browser-report-running.json`。

## 当前限制

500 页级别已通过程序分配与恢复规则测试，本版尚未完成 532 页真实转换；45 页成功不能冒充 532 页真实验收。当前也没有配置自动全篇语义复核。

最终关闭 code mode 的额外最小权限检查返回 `no_tool`，临时 canary 未被修改；该检查实际输入 2,607、输出 37、合计 2,644 token。模型自述和未出现工具调用都不能代替操作系统权限测试。Windows 原生沙箱读隔离的失败结果仍保留，不能宣称已经解决。

## 本次重构验证总用量

整篇 24 次、前置 6 页 5 次、能力检查 2 次，共 **31 次取得上游响应的请求，418,194 token**。另一次本地运行时配置错误在推理前失败。这里将准备测试也单独列出，不能把整篇账本的 24 次说成重构期间全部消费。

# v0.7 真实验证记录

2026-09-07。五步视觉转换，原生计数器及标签引用，最终由 xhigh 收尾员工统一处理标题、附录和异常引用。

## 完整论文

源文件：Wang（2022），45 页。运行 `be031dc93b6348079f43f3428d349727`，最终 `COMPLETED`，生成 55 页 PDF 和可下载 TeX 工程。

- 实际渠道：已登录官方订阅，gpt-6-astra，每批 3 页，共 15 个转换任务，并发 4。
- 转换、衔接 medium；设置、收尾、计数规则修复 xhigh。强度来自实际请求账本。
- 转换批次只收到自己的页图与公共规则。未使用 MinerU、旧版转换结果、目标登记表或引用登记表。
- 15 个转换任务全部一次完成，7 个边界请求。原生键未加批次前缀。
- 生成 175 个标签、383 处引用；初始 379 处可解析，无重名标签。收尾员工修正 4 处环境名不同的引用键，全部指向已有 Lemma 标签。
- 最终 175 个标签从 `.aux` 读取的实际编号均与其隐藏来源编号一致，包括 A/B 附录公式、引理和标题；383 处引用均能唯一解析。
- 原生 XeLaTeX 两轮通过；工程包含正文、公共导言区、标题定义、来源映射、图片资源和 PDF。45 个来源页标记完整、有序。
- 27 份回执均完成，没有工具调用项。没有人工改写论文正文或手动补数学公式。

## 真实测试暴露并修复的问题

前置 3 页不足以推断全篇所有计数规则；首次编译后设置员工补上按节编号的公式和独立全局 Claim 计数器。

最初标题接口没有附录切换位置，A/B 被排成 10/11，后续公式与引理也跟随错误。两个计数规则修复请求无法弥补这个结构接口缺口，程序保存 `COUNTER_MISMATCH`。

修复公共流程：现有收尾员工增加 `appendix_start` 标题 ID，程序在该处插入一次原生 `\appendix`。重启后复用全部转换与衔接，只重跑更新后的收尾任务，最终通过。**本次是一次真实运行经过代码修复和恢复完成，不是最终代码从头到尾首次无人值守成功的证明。**

## 实际用量

共 **27 次请求、425,155 token**：输入 345,515，输出 79,640。缓存输入 11,008 已包含在输入中；推理 3,930 已包含在输出中。总历时约 20 分 6 秒，包含测试中的修复与恢复间隔。

| 请求用途 | 次数 | 输入 token | 输出 token |
| --- | ---: | ---: | ---: |
| headings | 2 | 45,466 | 3,275 |
| counter_repair | 2 | 44,443 | 964 |
| seams | 7 | 76,936 | 301 |
| convert | 15 | 168,036 | 72,879 |
| setup | 1 | 10,634 | 2,221 |

两次收尾包含附录接口更新后的重跑，两次计数修复均计入成本，没有把失败前消耗扣除。临时连接并发 4 已恢复为测试前的 2。

## 程序与界面

40 项程序检查通过，包括实际 XeLaTeX 编译、共享计数、复位、前向引用、插入陈述后自然改号、附录 A/B 切换、同号按作用域消解、错误设置可重试，以及接缝不能绕过计数规则。

真实转换期间浏览器检查通过：阅读区加载 682 ms，未记录长任务；轮询期间没有额外图片请求，真实批次完成没有覆盖正在输入的页码。完成态再次检查加载 639 ms，同样没有长任务和图片重复请求。结构思考强度默认 xhigh；已移除的上下文／输出容量控件没有重新出现。

## 证据与范围

本地证据：`.bookanalyst/validation-v7.json`、运行目录 `runs/be031dc93b6348079f43f3428d349727/v7/` 和同级 `requests/` 原始回执。核心文件包括 `structure-edits.json`、`symbols.json`、`numbering-report.json`、`compile-report.json` 和 `tex/` 工程。

以上检查证明已生成标签的自然编号和引用一致，不证明视觉识别出的每个原书编号、每段数学内容均无误。原文错号、特殊编号形式、节选指向未包含对象的引用仍可能需要处理。Windows 原生跨目录读取隔离的既有限制仍适用，见 [v0.6 记录](validation-v0.6.md)。本次没有重新执行 532 页整书转换，也没有真实联调自定义 API。

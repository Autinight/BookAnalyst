# v0.7.1 收尾权限与真实恢复验证

2026-09-07。运行 `aebd35b6169845ddae22da632f811287`，Chan 等（2024），原始 45 页、每批 5 页、9 个批次。原状态因 `COUNTER_RULE` 停在第五步，本次最终 `COMPLETED`，输出 49 页 PDF。

收尾现在可以读取任意工程文件和原书页面，修改主文件、导言区、正文、标题、计数定义、标签与引用，并新建工程内 TeX 文件。程序接收精确补丁、保存工程检查点、编译并反馈。未增加针对某篇文献或某类参考文献的解析分支。

## 真实结果

- 原 9 个转换批次 SHA-256 全部未变，没有重跑转换。
- 全书计数规则修复后，剩余 28 个非数字参考文献标识暴露了旧默认数字列表与作者写法不符的问题。
- 获得工程修改权限后，agent 自主请求正文后段及第 43、44、45 页图像，返回对 `preamble.tex` 和 `body.tex` 的 29 处精确修改。修改内容来自真实模型回执，未由开发者手动修正文献产物。
- agent 在此工程内生成公共条目定义，保留原书引用代号并绑定原生 label/ref；程序没有内置这个格式。
- 162 个标签全部通过 `.aux` 实际标识核对，417 处引用可唯一解析；XeLaTeX 编译通过，PDF 和工程下载接口均返回 200。
- 联调暴露了多轮索取页图时旧上下文被覆盖的问题；已改为保留先前请求的图像和文件范围，并增加连续索取不同页的回归测试。

## 用量与范围

本次恢复新增 **8 次真实请求、146,565 token**：输入 130,130，输出 16,435，其中推理 9,740 已包含在输出中。全部使用 gpt-6-astra、xhigh。请求账本从 16 次累计到 24 次。费用统计包括中途权限不足的修复尝试和上下文重复读取，没有剔除这些消耗。

其中工程权限版本进行了 4 次读取上下文请求、1 次实际工程补丁请求；最终复用已保存补丁的恢复没有新增 LLM 请求。工程完成前经历了代码修复和暂停恢复，不能据此声称最终版本从零首次无人值守成功。上下文保留改动经过程序回归测试，本次最后一次真实恢复直接编译通过，未再次触发模型读取。

47 项相关测试通过；随后针对上下文保留的增强测试通过。标签与引用核对不等于数学内容已逐字验证无遗漏。

本地证据：`.bookanalyst/validation-v7-finisher.json`，运行的 `v7/finish-project.json`、`numbering-report.json`、`symbols.json`、`compile-report.json`、`tex/`，以及同级 `requests/` 中的原始回执。最终模型补丁请求：`10292b3c0c084826a6f68f4f97038894`。


## 目录前置规则补充

目录生成策略已纳入全局设置的现有 `rules` 与 `public_tex`；转换员工执行单次原生 `\tableofcontents`，原目录条目仅作为标题结构锚点。此改动未新增目录审查阶段，也未新增真实 LLM 请求。

49 项本地回归测试通过，包括实际 XeLaTeX 目录首次生成、已有目录更新、全局目录名称与展示层级，以及没有标签引用警告时仍通过 `.toc` 变化自动触发必要编译。该记录验证程序编译与规则传递的实现，不代表更新后的提示词已重新做过真实整书转换。

## 页面衔接并发修复（2026-09-07）

旧调度在循环中逐个等待接缝响应，未使用任务的 `llm_concurrency`。现已按任务并发数派发，并继续遵守连接的 `max_in_flight`；按原文顺序合并补丁，逐接缝保存进度。环境继承从转换结果标记的独立起始位置计算，避免远处错误影响无关接缝。实际发生文本重叠或继承环境变化时，才更新受影响的接缝。

8 项调度测试通过，覆盖并发 1/2/6、单页批次两端修改、环境依赖、重叠补丁、失败后保留在途结果及恢复复用。其余相关回归测试此前共 36 项通过。

使用 Atiyah–Macdonald 139 页任务 `0f178bb78d584506885c94512e076714` 已保存的 13 份真实模型接缝结果进行本地回放：峰值并发 6，13 处各处理一次，合并文档与原结果相同。本地回放通过受控异步等待验证调度，不代表新的上游并发实测；额外上游请求为 0。报告在本地 `.bookanalyst/validation-parallel-seams.json`。

该任务在排查期间完成页面衔接、进入收尾。当前正在运行的服务未强制重启，此代码更新在服务下次启动时生效。

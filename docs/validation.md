# 验证记录 v0.2

日期：2026-09-05。真实服务结果和模拟回归分别记录；模拟通过不表示真实书籍转换通过。

## 已完成的真实整书解析

用户已明确授权：完整 532 页测试书提交 MinerU，并使用现有官方订阅的 gpt-6-astra 转换和审查；本轮真实 LLM 请求累计上限 100。此前的单页授权已经扩展，后续恢复沿用同一运行和预算。

- 真实运行：`03c7a4c987cb4459ba331ebe4d8f21fb`。
- PDF 按 MinerU 接入限制分成 200／200／132 页，3 次实际解析提交，532 页全部完成并下载。
- 原始结果共 8375 个 block，包含 2498 个独立公式块、1016 个丢弃候选及 2501 条资源引用。丢弃候选不代表已经批准排除。
- JSON、原始资源、来源映射和提交回执保留在该运行目录。页分块只用于解析提交，模型分析按 block 批次执行。
- 已开始真实官方订阅推理；累计用量和实时状态以 SQLite 调用账本与运行界面为准。快照见 `tmp/book-scale/real-run-report.json`。
- 全书语义转换、原图核对和最终 TeX 验收尚未完成，不能把 S1 解析成功写成整书转换成功。

## 真实联调实际发现的问题

1. **结果 CDN 下载连接。** 系统代理 DNS 返回虚拟地址 198.18.0.111；公开 DNS 返回实际 CDN 地址后，保持原始 HTTPS Host 和 TLS SNI 校验即可下载。项目提供可选 `MINERU_DOWNLOAD_PUBLIC_DNS=1` 回退，只针对官方结果 CDN，未修改系统代理或关闭证书校验。
2. **MinerU 资源路径。** 真实 layout.json 的 image_path 使用裸文件名，ZIP 中资源位于 images/。规范化器已兼容裸文件名和显式相对路径，仍拒绝越界和同名内容歧义。已用真实单页与整书分块验证。
3. **S2 审查职责过宽。** 首次真实分析与独立审查把封面的坐标排列差异报为阅读顺序错误，并把已经保留的未知元素当作取舍阻断。现已限定 S2 判断内容成员关系：排除必须有明确依据，保守保留未知项；标题树和数学环境续接由 S3 检查，OCR 未知项交给原图核对。旧响应和失败记录保留，新修订重新审查。
4. **官方模型列表。** SDK 捆绑旧运行时返回 6 个模型；使用本机 PATH 中的官方 codex-cli 0.153.3 后，上游返回 7 个模型，默认 gpt-6-astra 并声明支持图像。界面使用实时列表，没有固定名称白名单。

## 整书规模程序回归

使用真实 532 页 PDF 完成物理分块，解析内容和模型响应由明确标记的本地语义测试服务提供，外部调用为零。它用于验证可以重复制造的故障与程序不变量：

- 2777 个 block、19 章、19 个目录锚点、533 条编号公式、19 个跨任务证明。
- 276 个增量结构分析批次、276 个语义转换任务；跨 MinerU 分块的公式关联物理页 200／201 并保持完整。
- 在结构分析和转换中各注入一次明确服务失败，恢复不重复已经通过的工作。
- 另行重跑一个中间转换任务，仅增加 4 次模拟模型调用：生成、独立审查和两侧边界审查。
- 运行并发设为 4，连接上限 3，观测到的在途请求峰值为 3。
- 使用 PATH 中的真实 XeLaTeX，生成 345 页重新排版的测试文档，目录、作者编号及标签检查通过。该文档包含模拟数学内容，不是原书的转换结果。

详细指标见 `tmp/book-scale/orchestration-report.json`。10000 项待处理队列的停止派发、错误环境续接、目录歧义、视觉中断后复用及内容变更后的局部重查也有独立回归。

## 本地界面与打包

Microsoft Edge 浏览器回归已验证整本书创建入口、上游动态模型列表、明确填写预算、离线夹具执行、原页／解析／TeX 定位、S7 重跑、导出和设置保存。检查了宽屏及 390px 窄屏，公式渲染使用包内 KaTeX。

安装包包括 Python 工作流模块、页面资源、KaTeX 和字体。当前完整测试数量和构建结果见文末最新检查记录。

## 原有单页测试

原单页运行 `97a7adef54cc4818a473f6b68f3d654e` 只提交过一次 PDF 第 14 页，远端批次 `3c2d6c4e-d2a4-4a08-97d5-9e9dfbd3f558`。其真实结果现已通过修复后的下载器取回；没有重复提交该页任务。

第 14 页人工预录夹具仍用于本地界面与编译回归，明确标为 fixture；它与真实 MinerU、真实模型响应分开保存。

## 尚未完成

全书真实语义转换和原图核对、复杂图表／特殊数学宏的真实验收、本机 MinerU／vLLM 部署尚未完成。程序检查和模型审查不能作为内容绝对无误的证明。

## 最新程序检查

完整 Python 回归：**53 passed**，包含真实联调后增加的 DNS 与资源路径兼容用例，以及整书规模故障恢复测试。两个警告来自第三方 TestClient／AnyIO 弃用接口。
